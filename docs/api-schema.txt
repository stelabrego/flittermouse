https://stackoverflow.com/a/4028874/9091281

POST /user/add/ creates new user
    req
    {
        username: string
        email: string
        password: string
        inviteKey: string TODO
    }
    res
    {
        status: bool
        message: string
        userKey: string optional
    }

PUT /users/update updates user
    req {
        userKey: string
        username: string optional
        email: string optional
        password: string optional
        displayName: string optional
        phoneNumber: string optional
        address: string optional
        bio: string optional
    }
    res
    {
        status: bool
        message: string
    }

DELETE /users/delete deletes user
    req {
        userKey: string
    }
    res
    {
        status: bool
        message: string
    }

PUT /users/privacy/update updates user privacy
    req {
        userKey: string,
        subscribedEventsVisibility: string optional
        addressVisibility string optional
        nameVisibility: string optional
        emailVisibility: string optional
        phoneNumberVisibility: string optional
    }
    res
    {
        status: bool
        message: string
    }

POST /event/add/ creates new event
    req
    {
        name: string
        userKey: string
        location: string optional
        dateOf: string optional
    }
    res
    {
        status: bool
        message: string
        eventKey: string optional
    }

PUT /events/update updates event
    req {
        eventKey: string
        name: string optional
        location: string optional
        dateOf: string optional
    }
    res
    {
        status: bool
        message: string
    }

DELETE /events/delete deletes event
    req {
        eventKey: string
    }
    res
    {
        status: bool
        message: string
    }

PUT /events/privacy/update updates event privacy
    req {
        eventKey: string,
        displayAddress: string optional
        displayDate: string optional
        visibility: string optional
    }
    res
    {
        status: bool
        message: string
    }


GET / returns homepage
GET /:username returns user profile page
GET /users/:userKey redirects to /:username
GET /signup returns "sorry invite only rn"
GET /signup/:inviteKey returns signup page, let user know if inviteKey is valid
GET /events/:eventKey returns event page